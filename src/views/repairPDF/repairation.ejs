<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>
    <%= title || 'Repair PDF' %>
  </title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/output.css" rel="stylesheet">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap');

    :root {
      --primary: #2563eb;
      --primary-hover: #1d4ed8;
      --glass-bg: rgba(255, 255, 255, 0.7);
      --glass-border: rgba(255, 255, 255, 0.5);
    }

    body {
      font-family: 'Outfit', sans-serif;
      background: radial-gradient(circle at top right, #eff6ff, #f8fafc);
      overflow-x: hidden;
    }

    .glass-card {
      background: var(--glass-bg);
      backdrop-filter: blur(12px);
      border: 1px solid var(--glass-border);
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.04);
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary), #60a5fa);
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 15px rgba(37, 99, 235, 0.2);
    }

    .status-badge {
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.7rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }
  </style>
</head>

<body class="min-h-screen flex flex-col">

  <header class="sticky top-0 z-50">
    <%- include('../partials/nav') %>
  </header>

  <main class="flex-1 max-w-7xl mx-auto px-6 py-12 w-full flex flex-col items-center">

    <!-- HEADER -->
    <div class="text-center mb-10 max-w-2xl">
      <h1 class="text-4xl md:text-5xl font-black text-slate-800 tracking-tight mb-4">
        Restore Your PDF
      </h1>
      <p class="text-slate-500 text-lg leading-relaxed">
        Detect corruption, rebuild file structures, and salvage readable content safely.
      </p>
    </div>

    <div class="w-full max-w-2xl relative">
      <!-- Decorative -->
      <div class="absolute -top-10 -right-10 w-32 h-32 bg-blue-100 rounded-full blur-3xl opacity-60"></div>
      <div class="absolute -bottom-10 -left-10 w-40 h-40 bg-indigo-100 rounded-full blur-3xl opacity-60"></div>

      <div class="glass-card rounded-3xl p-8 relative z-10">

        <!-- Error Message -->
        <% if (error) { %>
          <div class="mb-6 flex items-center gap-3 bg-rose-50 border border-rose-100 p-4 rounded-xl text-rose-600">
            <svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd"
                d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z"
                clip-rule="evenodd"></path>
            </svg>
            <p class="text-sm font-bold">
              <%= error %>
            </p>
          </div>
          <% } %>

            <!-- UPLOAD -->
            <form action="/repair/upload" method="POST" enctype="multipart/form-data" class="space-y-6">
              <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest mb-2 px-1">Upload Corrupted
                File</label>
              <input type="file" name="pdf" accept=".pdf" required
                class="block w-full text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-bold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100 cursor-pointer" />

              <button type="submit"
                class="btn-primary w-full text-white py-4 rounded-2xl font-black uppercase tracking-widest shadow-lg">
                Initialize Repair
              </button>
            </form>

            <% if (result) { %>
              <!-- RESULTS -->
              <div class="mt-12 pt-8 border-t border-slate-100 space-y-8 animate-in fade-in duration-500">

                <div class="flex justify-between items-center">
                  <h3 class="text-xs font-black text-slate-400 uppercase tracking-widest">
                    Diagnostic Report
                  </h3>

                  <% if (result.status==='Fully repaired' ) { %>
                    <span class="status-badge bg-green-100 text-green-700">Perfectly Restored</span>
                    <% } else if (result.status==='Partially recovered' ) { %>
                      <span class="status-badge bg-amber-100 text-amber-700">Partial Recovery</span>
                      <% } else { %>
                        <span class="status-badge bg-rose-100 text-rose-700">Unrecoverable</span>
                        <% } %>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div class="bg-slate-50/50 p-4 rounded-xl border border-slate-100">
                    <p class="text-[10px] uppercase font-bold text-slate-400 mb-1">Corruption Type</p>
                    <p class="font-black text-slate-700 tracking-tight">
                      <%= result.corruptionType || 'N/A' %>
                    </p>
                  </div>

                  <div class="bg-slate-50/50 p-4 rounded-xl border border-slate-100">
                    <p class="text-[10px] uppercase font-bold text-slate-400 mb-1">Recovery Engine</p>
                    <p class="font-black text-slate-700 tracking-tight">
                      <%= result.repairMethod || 'N/A' %>
                    </p>
                  </div>

                  <div class="bg-slate-50/50 p-4 rounded-xl border border-slate-100">
                    <p class="text-[10px] uppercase font-bold text-slate-400 mb-1">Page Count</p>
                    <p class="font-black text-slate-700 tracking-tight">
                      <%= result.pages %> pages
                    </p>
                  </div>

                  <div class="bg-slate-50/50 p-4 rounded-xl border border-slate-100">
                    <p class="text-[10px] uppercase font-bold text-slate-400 mb-1">File Size</p>
                    <p class="font-black text-slate-700 tracking-tight">
                      <%= result.fileSize %> KB
                    </p>
                  </div>
                </div>

                <div>
                  <div class="flex justify-between items-end mb-2 px-1">
                    <span class="text-[10px] uppercase font-bold text-slate-400">Restoration Confidence</span>
                    <span class="text-sm font-black text-blue-600">
                      <%= result.confidence %>%
                    </span>
                  </div>

                  <div class="w-full bg-slate-100 h-2.5 rounded-full overflow-hidden">
                    <div class="h-full bg-blue-500 rounded-full transition-all duration-1000"
                      style="width:<%= result.confidence %>%"></div>
                  </div>
                </div>

                <% if (result.downloadUrl) { %>
                  <div class="bg-blue-600/5 p-6 rounded-3xl border border-blue-100 text-center">
                    <div class="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-3">
                      <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                          d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
                      </svg>
                    </div>
                    <p class="text-sm font-bold text-slate-700 mb-1">
                      File Rebuilt Successfully
                    </p>
                    <p class="text-[11px] text-slate-400 mb-4 italic">Your download should start in a moment...</p>

                    <a href="<%= result.downloadUrl %>" id="download-link"
                      class="inline-block text-blue-600 font-black uppercase text-xs tracking-widest hover:text-blue-700">
                      Manual Download
                    </a>
                  </div>
                  <% } %>

              </div>
              <% } %>

      </div>
    </div>
  </main>

  <script>
  window.addEventListener("DOMded", () => {
    const link = document.getElementById("download-link");
    if (link) {
      setTimeout(() => link.click(), 1200);
    }
  });
  </script>

</body>

</html>